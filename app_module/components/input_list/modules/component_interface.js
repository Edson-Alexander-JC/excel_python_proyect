
if (!window.MyApp.ComponentInterface) {

    window.MyApp.ComponentInterface = class {
        const {
            parentElement,
            setStateValue,
            setTriggerValue,
            data
        } = component;
    }
}